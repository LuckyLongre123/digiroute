/**
 * /a/[slug]: Non-blocking Route Loading Boundary
 *
 * Progressive granular loading is handled directly inside page.tsx
 * so the user immediately sees the UI shell without a monolithic block.
 */
export default function RecipientLoading() {
  return null;
}
